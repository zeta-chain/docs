import { ContractAddresses } from "~/components/Docs";
import { Alert } from "~/components/shared";

# Contract Addresses

<Alert variant="tip">For a list of RPC endpoints, refer to the [API endpoints page](/reference/api).</Alert>

<ContractAddresses />
